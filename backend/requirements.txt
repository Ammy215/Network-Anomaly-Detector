fastapi
uvicorn[standard]
pydantic-settings
python-dotenv
python-multipart
pyshark
supabase
pyparsing>=3.1.0,<4  # supabase -> storage3 -> pyiceberg needs DelimitedList (pyparsing>=3.1); an older 2.x can get resolved in otherwise and crashes backend import
httpx  # already a transitive dep via supabase; used directly for threat-intel provider calls
scikit-learn
chromadb  # local vector DB for RAG retrieval; bundled embedding model runs via onnxruntime, no API key
langgraph  # Phase 7 -- orchestrates the classify/retrieve/explain/self_check investigation pipeline
groq  # Phase 7 -- LLM provider; free tier, no-training-on-data policy (see docs/PROJECT.md decision log)
pyjwt  # Phase 9 -- verifies Supabase Auth JWTs (ES256) against the project's public JWKS endpoint via PyJWKClient
pytest
