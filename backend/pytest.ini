[pytest]
markers =
    llm: hits the real Groq API (Phase 7). Excluded from the default run so
        routine test passes don't spend free-tier quota; run explicitly
        with `pytest -m llm`.
addopts = -m "not llm"
